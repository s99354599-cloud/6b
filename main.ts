basic.forever(function () {
    if (true) {
        basic.showIcon(IconNames.SmallHeart)
        radio.sendNumber(0 + 0)
        led.toggle(0, 0)
        led.unplot(0, 0)
        radio.sendNumber(0)
    }
})
